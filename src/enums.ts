export enum Category {
	MONSTERS = "monsters",
	EQUIPMENT = "equipment",
	MATERIALS = "materials",
	CREATURES = "creatures",
	TREASURE = "treasure"
}

export enum URLParams {
	QUERY = "query",
	CATEGORY = "category",
	PAGE = "page"
}
